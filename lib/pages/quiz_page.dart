import 'package:flutter/material.dart';

class QuizPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Quizz Mathématiques')),
      body: Center(
        child: Text('Page de Quizz à développer'),
      ),
    );
  }
}
